<li>
  <%= gravatar_for user, size: Settings.index_html.size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("users.user_html.delete"), user, method: :delete,
        data: { confirm: t("users.user_html.confirm")} %>
  <% end %>
</li>
